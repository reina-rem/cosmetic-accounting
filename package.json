{
  "name": "cosmetic-accounting",
  "version": "0.0.1",
  "scripts": {
    "start": "cross-env NODE_ENV=production node dist/src/main.js",
    "start:dev": "cross-env NODE_ENV=development nest start --watch",
    "populate-db": "cross-env NODE_ENV=development ts-node src/db/seed/populate-db.ts",

    "build": "nest build",
    "start:debug": "cross-env NODE_ENV=development nest start --debug --watch",

    "typeorm": "npx typeorm-ts-node-commonjs",
    "migration:generate": "cross-env NODE_ENV=development-cli npm run typeorm migration:generate -- ./src/migrations/AutoGeneratedMigration -d ./src/db/data-source.ts",
    "migration:run:dev": "cross-env NODE_ENV=development-cli npm run typeorm migration:run -- -d ./src/db/data-source.ts",
    "migration:run:prod": "cross-env NODE_ENV=production-cli npm run typeorm migration:run -- -d ./src/db/data-source.ts",
    "migration:revert:dev": "cross-env NODE_ENV=development-cli npm run typeorm migration:revert -- -d ./src/db/data-source.ts",
    "migration:revert:prod": "cross-env NODE_ENV=production-cli npm run typeorm migration:revert -- -d ./src/db/data-source.ts"
  },
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/config": "^3.3.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/jwt": "^10.2.0",
    "@nestjs/platform-express": "^10.0.0",
    "@nestjs/swagger": "^8.1.0",
    "@nestjs/typeorm": "^10.0.2",
    "@types/bcryptjs": "^2.4.6",
    "array-unique": "^0.3.2",
    "bcryptjs": "^2.4.3",
    "class-validator": "^0.14.1",
    "cookie-parser": "^1.4.7",
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.5",
    "pg": "^8.13.1",
    "reflect-metadata": "^0.2.2",
    "swagger-ui-express": "^5.0.1",
    "typeorm": "^0.3.20",
    "typeorm-naming-strategies": "^4.1.0"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.0.0",
    "@types/express": "^5.0.0",
    "@types/node": "^20.3.1",
    "ts-loader": "^9.4.3",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.1.3"
  }
}
